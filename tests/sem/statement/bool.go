// 3 consider extension 0
package main

func main () {
	a := true
	b := false
	res_and:= a && b
	// res_or:= a || b

	print("a(", a,") , ","b(", b,")\n")
	// print("a(", a,") && ","b(", b,")= ", res_and, "\n")
	// print("a(", a,") || ","b(", b,")= ", res_or, "\n")
}
