
/**
 * @file template.h
 * 
 * Templates for assembler
 * 
 * @brief IFJ Projekt 2020, Tým 58
 * 
 * @author <xbobro01> Bobrov Ivan
 * @author <xcaras00> Carasec Elena
 * @author <xkozhe00> Kozhevnikv Dmitrii
 * @author <xsenic00> Senichak Yahor
 */

/**
 * Writes the top of the program start
 */
void print_head();

/**
 * A template for concatenation
 */
void template_str_concat();

/**
 * Template for checking division by zero
 */
void template_div_by_zero()ж

/**
 * Reading a string from an input
 */
void template_inputs();

/**
 * Reading a integer number from an input
 */
void template_inputi();

/**
 * Reading a floating-point number from the input
 */
void template_inputf();

/**
 * Read a Boolean from the input below
 */
void template_inputb();

/**
 * Writing out a parameter
 * @param i Number of parameters
 * @param func_call_num Parameter number
 */
void template_print(unsigned i, unsigned func_call_num);

/**
 * Converts integer number to floating-point number
 */
void template_int2float();

/**
 * Converts floating-point number to integer number
 */
void template_float2int();

/**
 * Calculating the string length
 */
void template_len();

/**
 * Calculating the substring in a string for the required number of symbols
 */
void template_substr();

/**
 * Calculating the ASCII symbol value at a position in a string
 */
void template_ord();

/**
 * Calculating a symbol by its number in ASCII table
 */
void template_chr();
